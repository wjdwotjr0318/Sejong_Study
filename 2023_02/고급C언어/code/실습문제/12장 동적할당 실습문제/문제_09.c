#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main() {
	int n;
	scanf("%d", &n);
	getchar();
	char** str = (char**)malloc(sizeof(char*) * n);
	int i, j;
	for (int i = 0; i < n; i++) {
		for(int j = i;)
	}
	return 0;
}