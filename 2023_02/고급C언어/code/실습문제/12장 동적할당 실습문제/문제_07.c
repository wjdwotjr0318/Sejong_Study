#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main() {
	int n, m;
	scanf("%d %D", &n, &m);
	char** p = NULL;
	p = (char**)malloc(m * sizeof(char*));

	return 0;
}